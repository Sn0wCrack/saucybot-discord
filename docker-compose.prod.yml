version: "3.8"

services:
    bot:
        image: ghcr.io/sn0wcrack/saucybot-discord:1
        env_file:
            - .env
        restart: unless-stopped
    cache:
        image: redis:7-alpine
        restart: unless-stopped
        volumes:
            - cache:/data

volumes:
    cache:
